---
type: moc
---

# Concepts & Safety Notes

The actual knowledge graph — herbs, practices, and evidence notes referenced across both traditions. This list is what makes the vault worth having: every note below is linked from multiple phases/days, which is exactly the duplication the old `data/*.js` model couldn't avoid.

```dataview
LIST
FROM ""
WHERE type = "concept" OR type = "safety"
SORT file.name ASC
```

Bridging notes (used by *both* traditions — the graph's clearest payoff): [[Ginger (Halia Tua · 老姜)]].
